# skills

One folder per skill. The `skills` CLI walks this directory and installs each folder containing a
`SKILL.md` into whichever agents it detects.

Required shape and the rules every skill follows: [../AGENTS.md](../AGENTS.md).

Nothing has landed yet. The port order and the source of each skill are tracked in the design notes.
